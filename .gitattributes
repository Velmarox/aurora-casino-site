# Overrides the machine-wide core.autocrlf=true so this repo stays LF everywhere.
* text=auto eol=lf

# Binary — never touch these
*.png  binary
*.jpg  binary
*.jpeg binary
*.avif binary
*.mp4  binary
*.ico  binary
*.zip  binary
